SELECT DISTINCT maker
FROM Product
WHERE type = 'Printer'
  AND maker IN (
    SELECT pro.maker
    FROM Product as pro
    JOIN PC as pc ON pro.model = pc.model
    WHERE pc.ram = (SELECT MIN(ram) FROM PC) 
      AND pc.speed = (SELECT MAX(speed) FROM PC WHERE ram = (SELECT MIN(ram) FROM PC))
  );